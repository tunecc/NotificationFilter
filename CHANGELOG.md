# Changelog

## v1.3.8 - 2026-08-25

### 问题修复

- 修复 1.3.7 在 Dopamine2-roothide + iOS 15 上导致 SpringBoard 安全模式循环（卡注销圈圈、无法进入桌面）的问题：1.3.7 在路径解析时把旧版本留在可见 `/var/mobile/Library/Preferences/` 的通知历史文件跨容器 move 进随机化的 `.jbroot` 容器并删除可见残留，这一搬运正是崩溃根源；现历史文件路径改为与 NFPreferences / NFLogStore 相同的模式——直接写入 `jbroot()` 解析后的隐藏路径（`.jbroot`（roothide）/ jb 根（rootless）/ 原样（rootful）），不做任何迁移与删除
- 隐藏效果保留 1.3.7 的目标：新的通知历史文件（history / history-request / history-status）不再写入可见的 `/var/mobile/Library/Preferences/`；旧版本留下的可见历史文件保留在磁盘上但不再读取，需要时可手动恢复

## v1.3.7 - 2026-08-24

### 问题修复

- 修复 roothide 下三个通知历史文件（history / history-request / history-status）持续出现在可见的 `/var/mobile/Library/Preferences/` 的问题：通知历史桥接层的 `jbroot()` 在 roothide 构建中被编译期展开为原样返回，路径从未翻译到隐藏的 `.jbroot` 目录；现对齐 include 顺序，`jbroot` 恢复为对 libroothide 真函数的调用，三个文件改写入隐藏路径，不再暴露于可见目录（rootful / rootless 构建不受影响）

### 改进优化

- 通知历史文件路径解析时自动清理历史残留：当隐藏路径（scoped）文件已存在且可见路径（legacy）残留旧文件时，删除 legacy 残留；升级后无需手动清理旧版本留在可见目录的文件

## v1.3.6 - 2026-08-23

### 改进优化

- 被拦截通知日志改为内存缓存合并落盘：原先每条被拦截通知都会整读、整写一次日志文件，通知集中回灌（如注销/重启后通知队列重放）时会造成明显的磁盘写放大；现在同一时间窗口内的多条日志合并为一次读写，显著减少磁盘写入量与电量消耗
- 日志落盘在写入前重新读取清空标记与日志数量上限，与应用端（设置中清空日志、调整上限）的最新状态保持一致，已清空的条目不会被延迟写入复活
- 「清空全部日志」「清空该应用日志」「按上限裁剪」会先落盘未写入的日志再执行，并同步作废缓存，保证清理立即生效

### 行为变化

- 日志落盘由立即写入改为最多 2 秒的短延迟合并：设置中的日志视图最坏滞后约 2 秒显示最新条目；SpringBoard 异常退出时至多丢失最近 2 秒内尚未落盘的日志条目（诊断性日志数据，影响可忽略）

## v1.3.5 - 2026-08-23

### 功能新增

- 单应用规则页新增「导出该应用规则」，可把当前应用的规则导出为 JSON，复制到剪贴板或通过分享面板发送
- 设置页导入/导出新增「选择应用规则导出」，可单选或多选已配置规则的应用，批量导出为一份 JSON
- 新增类型化导入管线，自动识别导出内容类别（单应用规则 / 多应用规则 / 完整配置）并给出对应的导入结果提示；剪贴板导入与手动粘贴导入共用同一处理流程
- 单应用规则导入按应用整体替换对应规则，不影响全局规则、日志开关与日志数量上限等配置
- 旧版无类型标识的导出 JSON 仍按完整配置识别并覆盖导入，保持向后兼容
- 新增载荷结构与导入合并/覆盖/错误语义的单元测试

### 改进优化

- 调整单应用规则页布局：「导出该应用规则」移至「删除该应用规则」上方，导出页脚说明同步归位；导出行不再显示左侧图标，与其他行保持一致

## v1.3.4 - 2026-08-09

### 功能新增

- 新增全局「记录拦截日志」开关，可一键暂停被拦截通知的日志记录；仅影响之后写入的日志，不影响拦截行为，也不清理已存在的历史日志
- 新增单应用「记录该应用日志」开关，在「应用规则」页与「已过滤通知」详情页两处入口共享同一偏好、实时联动
- 全局与单应用两级组合生效：仅当全局日志开启且该应用未被禁用时才记录被拦截通知

### 问题修复

- 修复发布包内文件权限被 theos 保留为 0600，导致 mobile 用户无法读取、其他 tweak 扫描 PreferenceLoader plist 时设置页崩溃的问题，打包时统一规范为文件 0644 / 目录 0755
- 补充缺失的英文 `LOGS_CLEAR_APP_MESSAGE` 本地化文案

### 发布维护

- 移除构建脚本中硬编码的 Xcode-14.2.0 SDK 兼容处理，消除本地与 CI 构建模块配置不一致的问题

## v1.3.3 - 2026-07-21

### 问题修复

- 修复浅色模式下已添加规则卡片与列表底色同为灰色、层次感不足的问题，卡片改为系统 Grouped 次级背景色，呈现白卡片浮在灰底上的层次

## v1.3.2 - 2026-07-04

### 问题修复

- 修复深色模式下首次进入“应用规则”选择页时背景偶发显示为灰色的问题，统一首次进入、再次进入和加载中的页面底色
- 修复“应用规则”选择页在空列表和搜索无结果时只显示空白表格的问题，新增明确的空状态提示文案

## v1.3.1 - 2026-07-02

### 功能新增

- 规则列表编辑模式新增“全选 / 取消全选”，批量删除时可一次选择全部规则
- 通知扫描选词页按标题、副标题、正文分区新增“全选 / 取消全选”，便于整段快速选词生成规则

### 问题修复

- 修复扫描选词跨作用域保存时被错误合并为单条规则的问题，改为按所选分区分别生成并保留原始作用域
- 修复白名单“默认拦截”场景下部分通知共用去重键导致的误去重问题，并补充对应回归测试

## v1.3.0 - 2026-07-01

### 功能新增

- 单应用规则新增黑名单/白名单模式，每个应用可独立选择过滤策略
- 白名单模式支持“放行关键词”和“正则放行”，未命中白名单的通知会默认拦截
- 白名单模式支持“强制拦截”，可在放行规则命中前继续过滤指定通知

### 改进优化

- 单应用规则页新增“规则模式”设置，并根据当前模式动态显示规则名称、说明、输入提示和扫描生成规则类型
- 已过滤通知日志新增“白名单默认拦截”说明，便于区分未命中白名单导致的过滤结果
- 旧版应用规则会自动按黑名单模式兼容，现有规则行为保持不变

### 发布维护

- 为规则模式归一化和黑/白名单过滤流程补充回归测试

## v1.2.4 - 2026-06-23

### 问题修复

- 添加通知历史功能的异常保护，防止在旧版 iOS/RootHide 环境下因 API 不兼容导致崩溃
- 所有通知历史相关的功能调用现在都使用 try-catch 保护，确保核心过滤功能不受影响
- 即使通知历史扫描功能在特定环境下不可用，通知过滤功能仍能正常工作

## v1.2.3 - 2026-06-20

### 问题修复

- 修正 rootful、rootless、roothide 三种环境下的配置文件真实存储路径处理
- 主配置改为显式按文件读写，避免继续依赖 `CFPreferences` 隐式落盘导致路径不可控
- 为主配置及通知历史相关 plist 补齐旧路径兼容读取与迁移逻辑，统一收敛到当前规范路径

## v1.2.2 - 2026-06-06

### 改进优化

- 优化浅色模式下的规则显示

## v1.2.1 - 2026-05-26

### 问题修复

- 修复已过滤通知日志在旧通知被重复分发时可能产生大量重复记录的问题

## v1.2.0 - 2026-05-23

### 功能新增

- 扫描选词支持中文按单字拆分、英文和数字按单词拆分，更适合通知关键词规则生成
- 选中的不连续字词现在会按原顺序拼接为一条规则，跨标题/副标题/正文选择时自动回落为全文作用域匹配
- 扫描选词支持按住滑动连续选择，跨行滑动可继续选中，再次滑过已选项可直接取消

### 改进优化

- 扫描选词界面改为更紧凑的流式布局，提升单字分词场景下的空间利用率
- 优化选词按钮尺寸、触控热区、选中反馈和无障碍状态，提升连续操作体验
- 重构扫描分词与规则拼装逻辑，统一由独立 builder 负责，便于后续维护和扩展

### 问题修复

- 修复扫描后大爆炸选词存在漏字的问题
- 修复扫描保存时会把非连续选中字词拆成多条规则的问题
- 修复构建发布包时可能混入宿主机元数据导致 `.deb` 校验失败的问题

### 发布维护

- 为扫描分词和规则拼装补充独立测试覆盖
- 加固原生打包脚本，增加构建锁、宿主元数据清理和 deb 二次净化流程

## v1.1.0 - 2026-05-19

### 功能新增

- 新增通知中心历史扫描规则生成，支持从通知中心历史中选词生成包含/排除规则
- 应用规则页新增扫描按钮，进入时自动请求刷新当前 App 通知历史
- 支持按应用清空过滤日志

### 改进优化

- 作用域"消息"改为"正文"，更符合实际语义
- 优化扫描生成规则体验：按标题、副标题、正文区域展示分词结果，自动保存规则作用域

### 发布维护

- 规范 rootful/rootless/roothide 原生 Theos scheme 构建打包脚本
- 新增 GitHub Actions 自动构建发布流程

## v1.0.1 - 2026-05-06

### 改进优化

- 优化规则编辑体验并统一交互细节

## v1.0.0 - 2026-05-01

### 功能新增

- 通知过滤初版：支持标题/副标题/正文关键词匹配拦截
- 规则系统支持字段作用域匹配（标题、副标题、正文）
- 多语言适配（中文/英文）
- 支持自定义过滤日志保存条数
- 应用规则页支持编辑模式批量选择与删除确认

### 改进优化

- 统一 NotificationFilter 命名并更新偏好页图标与文案
- 优化过滤日志列表展示与过滤状态管理
- 右侧显示已过滤条数与预览

### 问题修复

- 补齐通知过滤在 Bulletin/Update 链路中的拦截与去重
- 优化过滤通知的通知中心删除逻辑
